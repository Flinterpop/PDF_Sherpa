# Python
__pycache__/
*.py[cod]

# PyInstaller / build outputs
build/
dist/
# Compiled installer -- published as a GitHub Release asset, not committed
installer/

# Local editor / tool settings
.claude/

# App data: the bundled/working PDF set and generated topic files
# (documents and build artifacts, not source)
pdfs/
